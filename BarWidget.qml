import QtQuick

// Milestone 0: a deliberately inert, valid bar-widget entry point.
// Panel behavior is added after backend capture and planning are tested.
Item {
    implicitWidth: 0
    implicitHeight: 0
}
